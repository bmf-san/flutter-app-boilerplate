import 'package:flutter/material.dart';

/// App-level semantic color tokens.
/// Add more as needed.
class AppColors {
  AppColors._();

  static const primary = Color(0xFF0F172A);      // slate-900
  static const secondary = Color(0xFF64748B);    // slate-500
  static const error = Color(0xFFEF4444);        // red-500
  static const success = Color(0xFF22C55E);      // green-500
  static const warning = Color(0xFFF59E0B);      // amber-500
  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFF1F5F9);      // slate-100
}
